import 'server-only';import{token}from'./session';const API=process.env.API_URL??'http://localhost:3001';export async function api(path:string,init:RequestInit={}){const bearer=await token();const headers=new Headers(init.headers);if(bearer)headers.set('Authorization',`Bearer ${bearer}`);headers.set('Accept','application/json');if(init.body)headers.set('Content-Type','application/json');return fetch(`${API}${path}`,{...init,headers,cache:'no-store'})}export async function apiJson<T>(path:string,init?:RequestInit):Promise<T>{const r=await api(path,init);if(!r.ok)throw new Error(`Backend request failed (${r.status})`);return r.json()}

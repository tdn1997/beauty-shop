import type {PrismaService} from '../../shared/infrastructure/prisma.service';import type {AuthStore} from '../application/auth.service';export class PrismaAuthStore implements AuthStore{constructor(private readonly prisma:PrismaService){}findUserByEmail(email:string){return this.prisma.user.findUnique({where:{email},select:{id:true,email:true,displayName:true,passwordHash:true,role:true,enabled:true}})}async createSession(data:Parameters<AuthStore['createSession']>[0]){await this.prisma.session.create({data})}findSession(tokenHash:string){return this.prisma.session.findUnique({where:{tokenHash},select:{id:true,expiresAt:true,revokedAt:true,user:{select:{id:true,email:true,displayName:true,role:true,enabled:true}}}})}async revokeSession(tokenHash:string,at:Date){await this.prisma.session.update({where:{tokenHash},data:{revokedAt:at}})}}

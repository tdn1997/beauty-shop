import{expect,it}from'vitest';import{MemoryLoginThrottle}from'./login-throttle';it('should bound login attempts by trusted source and normalized account key',()=>{const t=new MemoryLoginThrottle(()=>0,2,60000);expect(t.consume('socket','a@example.test').allowed).toBe(true);expect(t.consume('socket','a@example.test').allowed).toBe(true);expect(t.consume('socket','a@example.test').allowed).toBe(false);expect(t.consume('other','a@example.test').allowed).toBe(true)})
